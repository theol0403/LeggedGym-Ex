\section{Experiments and Results}
\label{sec:experiments}
% ====================================================================

We evaluate the DA2 pipeline through three experiments:
(1)~establishing upper-bound baselines with ground-truth sensing,
(2)~closing the gap between estimated and ground-truth depth by
varying model capacity and terrain texture, and
(3)~testing domain invariance under visual perturbations.
We report \emph{success rate} (fraction of episodes completing the
full course) and \emph{progress ratio} (mean fractional distance
along the course before termination), both averaged over 256
episodes.  

When analyzing visual robustness, we also report
\emph{depth signal quality} (DSQ), defined as the scale- and
offset-invariant Pearson correlation
between ground-truth and DA2-estimated depth images, averaged over
environments and time steps. 

\begin{equation}
  \mathrm{DSQ} = \frac{1}{T} \sum_{t=1}^{T}
    \frac{1}{N} \sum_{i=1}^{N}
      \frac{
        \sum_p (d^{\mathrm{GT}}_{i,p} - \bar{d}^{\mathrm{GT}}_i)
              (d^{\mathrm{DA2}}_{i,p} - \bar{d}^{\mathrm{DA2}}_i)
      }{
        \| d^{\mathrm{GT}}_i - \bar{d}^{\mathrm{GT}}_i \|_2 \;
        \| d^{\mathrm{DA2}}_i - \bar{d}^{\mathrm{DA2}}_i \|_2
      }
  \label{eq:dsq}
\end{equation}

% ====================================================================
\subsection{Baselines}
\label{sec:baselines}

We start by establishing upper bounds for the privileged teacher and the ground-truth depth sensing student (GT-depth).
The privileged teacher achieves near-perfect success across all gap difficulties, shown in Table~\ref{tab:baselines}.

The GT-depth student, receiving simulator depth images,
achieves success on all rows, confirming the CNN--GRU
architecture extracts sufficient geometry from a single depth viewpoint.
A zero-depth ablation (all depth pixels set to zero)
drops success to 8\%, verifying the policy relies on depth
rather than exploiting proprioceptive heuristics.

\begin{table}[h]
  \centering
  \caption{Baseline success rates on gap terrain (\%).}
  \label{tab:baselines}
  \small
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    & Row~0 & Row~1 & Row~2 & Row~3 \\
    \midrule
    Scandots teacher & 98.5 & 100.0 & 99.0 & 98.5 \\
    GT-depth student & 94.5 & 94.5 & 89.1 & 87.5 \\
    Zero-depth ablation & 8.0 & 2.0 & 0.0 & 0.0 \\
    \bottomrule
  \end{tabular}
\end{table}


% Figure~\ref{fig:teacher_training} shows training dynamics for both
% baseline stages. Reward rises steeply as the policy masters flat terrain, then continues
% climbing as the adaptive curriculum exposes progressively harder
% obstacles.
%
% \begin{figure}[h]
%   \centering
%   \includegraphics[width=\linewidth]{figures/teacher_training.pdf}
%   \caption{Baseline training curves.}
%   \label{fig:teacher_training}
% \end{figure}
%
%
% ====================================================================
\subsection{Parkour from Estimated Depth}
\label{sec:da2_results}

We now determine whether parkour can be achieved from estimated
depth. Starting from the GT-depth setup, we replace simulator
depth with DA2-small. 
As shown in Table~\ref{tab:da2_main}, 
DA2-small achieves success on early rows, but fails completely on Rows~2 and 3.
Switching to DA2-base results in promotion to Row 3, but it still falls well short of
GT-depth on the hardest rows.

The remaining limitation is the visual input. On untextured terrain,
the gap boundary provides weak cues, so the depth estimate is
ambiguous. As shown in Table~\ref{tab:da2_main},
adding a checkerboard terrain texture makes the gap geometry
visible and closes most of the remaining gap to GT-depth.
Figure~\ref{fig:da2_texture_comparison} shows examples of depth estimation
directly across representative surface conditions. 

\begin{table}[h]
  \centering
  \caption{Policy success rates (\%) during training as model scale and terrain texture are modified. }
  \label{tab:da2_main}
  \small
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    Configuration & Row~0 & Row~1 & Row~2 & Row~3 \\
    \midrule
    DA2-small & 56.3 & 33.6 & --- & --- \\
    DA2-base & 68.4 & 83.2 & 26.6 & 0.8 \\
    DA2-base + texture & 87.5 & 93.0 & 87.1 & 80.5 \\
    \midrule
    GT-depth baseline & 94.5 & 94.5 & 89.1 & 87.5 \\
    \bottomrule
  \end{tabular}
\end{table}


Figure~\ref{fig:student_success}
shows training dynamics, where reward rises steeply as the policies master flat terrain, then plateau
as the curriculum exposes progressively harder obstacles. 
The textured model is able to surpass the untextured models and approach the GT-depth's performance. 

% TODO: maybe progress ratio isn't right, why is depth lower?

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{figures/student_success_comparison.pdf}
  \includegraphics[width=\linewidth]{figures/per_obstacle_success.pdf}
  \caption{Success rate during student training. Breaking success down by obstacle shows most of the gain comes from gap traversal; stairs and hurdles are
less sensitive to texture because their geometry remains visible.
}
  \label{fig:student_success}
\end{figure}


\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/da2_texture_comparison.png}
  \caption{Robot-view comparison across representative terrain
    textures. Each panel shows rendered RGB (left) and the
    corresponding DA2 depth estimate (right). Featureless terrain
produces the weakest obstacle localization and the lowest DSQ, while
checkerboard, grass, and wood all preserve substantially
more geometric structure. }
  \label{fig:da2_texture_comparison}
\end{figure*}

% ====================================================================
\subsection{Domain Invariance}
\label{sec:domain}

We now test whether DA2 provides invariance to the visual domain
shifts encountered in sim-to-real transfer by perturbing the
trained student only at \emph{test time}.
Table~\ref{tab:domain_inv} reports success across six terrain
configurations (three obstacle families $\times$ two difficulty
levels) for texture replacement, lighting/color changes,
and combinations thereof, while Figure~\ref{fig:dsq_scatter}
plots DSQ against policy success.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/dsq_scatter.pdf}
  \caption{Success rate vs.\ DSQ across all six terrain configurations.
  Circles denote Row~0, squares Row~3.  Featureless
  surfaces cluster below DSQ\,=\,0.85 with catastrophic failure;
  all textured conditions maintain DSQ\,$>$\,0.85.}
  \label{fig:dsq_scatter}
\end{figure}

\begin{table}[h!]
  \centering
  \caption{Domain invariance: test-time success~(\%) under visual
  perturbations across obstacle types and difficulty levels
  (256 episodes per cell).}
  \label{tab:domain_inv}
  \small
  \begin{tabular}{@{}p{2.6cm}rr|rr|rr@{}}
    \toprule
    & \multicolumn{2}{c|}{Gaps} & \multicolumn{2}{c|}{Stairs}
    & \multicolumn{2}{c}{Hurdles} \\
    Perturbation & R0 & R3 & R0 & R3 & R0 & R3 \\
    \midrule
    Baseline & 96.1 & 94.1 & 100 & 92.2 & 96.9 & 94.9 \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Realistic surface textures}} \\
    \quad Concrete     & 79.3 & 12.1 & 99.6 & 25.8 & 95.7 & 73.1 \\
    \quad Stone tiles  & 93.0 & 78.9 & 100  & 96.9 & 95.3 & 90.6 \\
    \quad Gravel       & 91.4 & 48.8 & 100  & 93.4 & 93.4 & 34.8 \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Structured / featureless}} \\
    \quad Noise        & 82.8 & 29.3 & 100  & 95.3 & 97.3 & 47.7 \\
    \quad No texture   & 16.8 &  0.0 & 100  & 21.9 & 91.4 & 18.2 \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Brightness / color}} \\
    \quad Dim (0.3$\times$) & 82.8 & 58.2 & 99.6 & 94.1 & 99.6 & 95.7 \\
    \quad Color $\pm$30\%   & 79.3 & 32.0 & 99.6 & 94.9 & 97.3 & 87.9 \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Combined}} \\
    \quad Mild         & 76.6 &  8.2 & 100  & 15.2 & 97.3 & 79.3 \\
    \quad Extreme      & 34.0 &  0.4 & 99.6 & 66.8 & 89.5 & 19.1 \\
    \bottomrule
  \end{tabular}
\end{table}

The results reveal a clear obstacle-geometry hierarchy:
\emph{stairs} are nearly immune to perturbations at Row~0 because
step edges provide strong geometric cues regardless of surface
appearance, though Row~3 shows sensitivity.
\emph{Hurdles} at Row~0 are similarly robust (even no-texture
achieves 91.4\%), but Row~3 degrades substantially.
\emph{Gaps} are most sensitive because the boundary is visible
only through surface texture---there are no geometric edges---so
perturbation effects are large and scale with difficulty.

On easy gaps, stone tiles and gravel remain near baseline while
featureless surfaces fail catastrophically; on hard gaps the same
perturbations are amplified, approaching zero where Row~0 showed
only moderate degradation.  Lighting and color shifts cause modest
degradation, confirming DA2 is largely robust to photometric
variation.

As Figure~\ref{fig:dsq_scatter} shows, DSQ is a useful failure
indicator: featureless surfaces collapse both DSQ and success,
but high DSQ alone does not guarantee full robustness---some
unseen textures (e.g.\ wood, bricks on Gap~R0) reduce success
substantially despite preserving global depth structure.
We also computed an edge-weighted DSQ variant (Sobel-weighted
Pearson correlation) but found it does not improve prediction
over global DSQ ($r = 0.66$ vs.\ $r = 0.65$; $p < 0.001$
for both), suggesting that the performance gap for certain
textures arises from factors beyond edge-level depth accuracy.
